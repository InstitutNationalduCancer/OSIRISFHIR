version: 2
models:
  - name: osiris_clean_study_Endpoint
    description: "Mapping of OSIRIS_pivot_Study data to fhir path"
    columns:
      - name: "id_endpoint"
        description: "Mapping of Patient_Id+Instance_Id (Primary Key)"
        tests:
          - dbt_constraints.primary_key
      - name: "name"
        description: "Mapping of Study_Location"


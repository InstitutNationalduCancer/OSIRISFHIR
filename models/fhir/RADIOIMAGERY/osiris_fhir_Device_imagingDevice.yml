version: 2
models:
  - name: osiris_fhir_Device_imagingDevice
    description: Create the ImagingDevice profil from Endpoint resource (osiris_master_tab_Device)
    columns:
      - name: "id"
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: "fhir"
        description: Fhir format of the instance


version: 2
models:
  - name: osiris_fhir_Medication_imagingContrast
    description: Create the ImageryContrast profil from Medication resource (osiris_master_tab_Medication)
    columns:
      - name: "id"
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: "fhir"
        description: Fhir format of the instance


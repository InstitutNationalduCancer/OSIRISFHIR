version: 2
models:
  - name: osiris_fhir_Procedure_radiotherapyplan
    description: Create the RadiotherapyPhlan profil from Procedure resource (osiris_master_tab_Procedure_radiotherapyplan)
    columns:
      - name: id
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: fhir
        description: Fhir format of the instance

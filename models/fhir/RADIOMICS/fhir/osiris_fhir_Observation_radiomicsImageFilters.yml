version: 2
models:
  - name: osiris_fhir_Observation_radiomicsImageFilters
    description: Create the RadiomicsImageFilter profil from Observation resource (osiris_master_tab_Observation_radiomicsimagefilter)
    columns:
      - name: id
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: fhir
        description: Fhir format of the instance

version: 2
models:
  - name: osiris_fhir_Patient
    description: Create the Patient profil from Patient resource (osiris_master_tab_Patient)
    columns:
      - name: "id"
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: "fhir"
        description: Fhir format of the instance


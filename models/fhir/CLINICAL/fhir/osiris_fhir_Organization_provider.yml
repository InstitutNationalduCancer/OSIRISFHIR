version: 2
models:
  - name: osiris_fhir_Organization_provider
    description: Create the Provider profil from Organization resource (osiris_master_tab_Organization)
    columns:
      - name: "id"
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: "fhir"
        description: Fhir format of the instance


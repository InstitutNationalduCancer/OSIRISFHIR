version: 2
models:
  - name: osiris_fhir_Condition_tumorPathologyEvent
    description: Create the TumorPathologyEvent profil from Condition resource (osiris_master_tab_Condition_tumorPathologyEvent)
    columns:
      - name: "id"
        description: Id
        tests:
          - dbt_constraints.primary_key
      - name: "fhir"
        description: Fhir format of the instance

